{
  "name": "afroforma-functions",
  "version": "1.0.0",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google-cloud/storage": "^6.9.0",
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
